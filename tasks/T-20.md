# T-20: vidx-pipeline — runner

全9 Stage を連結。キャッシュ判定・進捗表示。

## 実装

```rust
pub async fn run_pipeline(ctx: JobContext) -> Result<(), VidxError>
```

Stage0～9 を順に実行。各Stage でキャッシュ判定。

## 受け入れ基準

- [ ] テスト pass
