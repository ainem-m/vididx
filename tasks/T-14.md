# T-14: vidx-segment — 意味分割(semantic)

LLM で意味境界推定。T-13 LLM クライアント使用。

## 実装

```rust
pub async fn semantic_chunk(
    llm_client: &AnthropicClient,
    coarse: &CoarseSegment,
    target_min: f64,
    target_max: f64,
) -> Result<Vec<SemanticChunk>, VidxError>
```

- プロンプト: `prompts/semantic_chunking.jinja`
- スキーマ検証 + フォールバック(1回リトライ後は等分割)

## 受け入れ基準

- [ ] テスト pass
