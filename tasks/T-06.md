# T-06: vidx-media — silencedetect

## 概要

ffmpeg silencedetect で無音区間を検出。

## 実装

```rust
pub async fn detect_silence(
    ffmpeg_path: &str,
    mp4_path: &Path,
    noise_db: f32,  // -30
    duration: f32,  // 0.6
) -> Result<Vec<SilenceInterval>, VidxError>
```

ffmpeg stderr をパース。

## 受け入れ基準

- [ ] `cargo test -p vidx-media` が全pass
