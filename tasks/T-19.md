# T-19: vidx-output — JSONL / Markdown

最終出力: chunks.jsonl + index.json + markdown

## 実装

```rust
pub async fn write_chunks_jsonl(...) -> Result<(), VidxError>
pub async fn write_markdown(...) -> Result<(), VidxError>
```

## 受け入れ基準

- [ ] テスト pass
