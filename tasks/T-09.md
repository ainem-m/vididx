# T-09: vididx-asr — adapter + whisper_cpp

## 概要

文字起こしの adapter trait と whisper.cpp 実装。

## 依存タスク

- T-01, T-04, T-05 完了済み

## 対象クレート

`crates/vididx-asr/`

## 実装ファイル

```
crates/vididx-asr/
├── Cargo.toml
└── src/
    ├── lib.rs
    ├── adapter.rs
    └── whisper_cpp.rs
```

## TDD手順

### Red フェーズ

- `AsrAdapter` trait で `transcribe()` メソッドが定義される
- `WhisperCppAdapter` が trait を実装する
- WAV ファイルを入力して `TranscriptTimeline` が返る
- 各セグメントに start_sec, end_sec, text が含まれている

### Green フェーズ

**adapter.rs**:
```rust
#[async_trait]
pub trait AsrAdapter: Send + Sync {
    async fn transcribe(&self, wav_path: &Path) -> Result<TranscriptTimeline, VididxError>;
}
```

**whisper_cpp.rs**:
```rust
pub struct WhisperCppAdapter {
    binary_path: String,
    model_path: String,
    threads: usize,
    language: String,
}

impl WhisperCppAdapter {
    pub fn new(binary_path: &str, model_path: &str, threads: usize, language: &str) -> Self
}

#[async_trait]
impl AsrAdapter for WhisperCppAdapter {
    async fn transcribe(&self, wav_path: &Path) -> Result<TranscriptTimeline, VididxError> {
        // Run: whisper-cli --model {model_path} --threads {threads} --language {language} --output-format json {wav_path}
        // Parse JSON output
    }
}
```

whisper-cli は JSON 出力 (`--output-format json`) をサポート。
出力フォーマット: `segments` 配列に各セグメント情報。

## 依存クレート

```toml
[dependencies]
vididx-core = { path = "../vididx-core" }
tokio = { workspace = true }
async-trait = { workspace = true }
serde_json = { workspace = true }
```

## 受け入れ基準

- [ ] `cargo test -p vididx-asr` が全pass
- [ ] `AsrAdapter` trait が定義されている
- [ ] `WhisperCppAdapter` が実装されている
- [ ] JSON パースが正しく動く
