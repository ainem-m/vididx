# T-15: vididx-segment — 正規化(normalize)

短すぎる結合・長すぎる再分割・chunk_id 付与。

## 実装

```rust
pub fn normalize(
    chunks: Vec<SemanticChunk>,
    hard_min: f64,
    hard_max: f64,
    video_id: &str,
) -> Vec<NormalizedChunk>
```

## ルール

1. < 15秒 → 隣接と結合
2. > 120秒 → 30-90秒目標で再分割
3. chunk_id: `{video_id}_chunk_{NNNN}`

## 受け入れ基準

- [ ] テスト pass
