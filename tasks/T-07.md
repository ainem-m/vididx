# T-07: vidx-media — scene change 検出

## 概要

ffmpeg scene change detection。

## 実装

```rust
pub async fn detect_scene_changes(
    ffmpeg_path: &str,
    mp4_path: &Path,
    threshold: f32,  // 0.4
) -> Result<Vec<SceneChange>, VidxError>
```

ffmpeg showinfo をパース。

## 受け入れ基準

- [ ] `cargo test -p vidx-media` が全pass
