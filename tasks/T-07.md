# T-07: vididx-media — scene change 検出

## 概要

ffmpeg scene change detection。

## 実装

```rust
pub async fn detect_scene_changes(
    ffmpeg_path: &str,
    mp4_path: &Path,
    threshold: f32,  // 0.4
) -> Result<Vec<SceneChange>, VididxError>
```

ffmpeg showinfo をパース。

## 受け入れ基準

- [ ] `cargo test -p vididx-media` が全pass
